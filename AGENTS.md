# OOM Agent Guide

## Project Identity

OOM (오픽온미, OPIc On Me)은 OPIc speaking practice를 위한 Vite + React + TypeScript 정적 웹앱이다. 현재 backend, database, authentication server는 없다. 녹음·TTS·설정은 browser가 소유하고, LLM/STT는 사용자가 설정한 endpoint를 browser에서 직접 호출한다.

기능 변경 전에는 이 문서를 기준으로 scope를 정한다. `reference/**`나 과거 audit 문서를 current product architecture로 사용하지 않는다.

## Read First

1. `README.md`
2. `AGENTS.md`
3. `docs/ARCHITECTURE.md`
4. `docs/TRAINING_SYSTEM.md`
5. 변경 종류에 따라 `docs/CONTENT_AUTHORING.md`, `docs/TTS_AUDIO_PIPELINE.md`, `docs/ROUTING.md`, `docs/DEPLOYMENT.md`
6. generated inventory가 필요하면 `docs/PROJECT_SNAPSHOT.md`와 `artifacts/tts-inventory.json`
7. affected component와 실제 `src/data/**` owner

## Source of Truth

| Concern | Source |
| --- | --- |
| Course discovery와 context resolution | `src/training/courseRegistry.ts`의 `discoveredCourses`, `resolveTrainingContext` |
| Level display/difficulty/time | `src/training/levels.ts`의 `TRAINING_LEVELS`, `formatTrainingPreset` |
| persisted selection | `src/training/TrainingSelectionContext.tsx`, `src/training/storage.ts` |
| active Course content | `src/data/training/courses/course-N/` |
| full survey structure와 rehearsal scoring | `src/data/fixedSurvey.ts` |
| Course 1 Advanced regression reference | `src/data/scripts.ts` |
| routes와 route-to-view mapping | `src/App.tsx`, `src/lib/routes.ts`, `src/components/layout/Sidebar.tsx` |
| sidebar hierarchy | `src/components/layout/ExpandableSidebar.tsx` |
| static SEO routes | `scripts/generate-static-routes.mjs` |
| static TTS inventory | `artifacts/tts-inventory.json`, generated by `npm run tts:audit` |
| production TTS manifest | `public/generated-tts/tts-manifest.json` |
| canonical documentation | `README.md`, `AGENTS.md`, `docs/ARCHITECTURE.md`, `docs/TRAINING_SYSTEM.md`, `docs/CONTENT_AUTHORING.md`, `docs/TTS_AUDIO_PIPELINE.md`, `docs/ROUTING.md`, `docs/DEPLOYMENT.md` |

## Architectural Invariants

- Course owns the content world: survey recommendation, canonical storylines, variants, replacement guides, roleplays, questions.
- Level owns difficulty, answer density, display label, and target duration.
- Never silently choose a fallback Course or Level. STEP 1 configures `TrainingSelection`; STEP 2~6 require a valid selection through `TrainingSelectionGuard`.
- Preserve the six-step flow and the sidebar nesting. STEP 4 and STEP 5 have child groups.
- Use the complete Level display contract from `src/training/levels.ts`: `1구간 · AL · 60~90초`, `2구간 · IH / IM3 · 45~65초`, `3구간 · IM2 / IM1 · 30~45초`.
- One canonical storyline per group per Course. Keep the same core scene across three Levels. Do not restore Story A/B selection.
- STEP 4 keeps one macro order at every Level: OPEN, SCENE, CLOSE. These are learning sections, not raw paragraph indexes. ANSWER, SCENE/ACTION/DETAILS, RESULT/FEELING, and optional EXPANSION remain micro functions.
- STEP 4 keeps the existing three tabs and shared variant selection. Foundation must never fall back to an Advanced paragraph.
- STEP 5 treats six named functions as a flexible menu. Problem/purpose, request/question, and next action are CORE; the rest are OPTIONAL. Roleplay count is independent of storyline count.
- STEP 6 is one parent training area with a hub at `/practice/`, Quick Practice at `/practice/quick/`, and Full Mock at `/practice/mock/`; all three remain STEP 6 at 100% progress. Do not collapse these child routes back into route-local mode state without an explicit product decision.
- Quick Practice keeps the one-question warm-up → exam → review/STT/AI → retry loop. Full Mock begins Background Survey → Self Assessment → Pre-Test → warm-up → Session 1 → difficulty adjustment → Session 2 → whole-session review. Both exam surfaces are audio-first, non-seekable, and capped at listen `0/2` through `2/2`.
- During Full Mock, do not call or show STT, AI, transcript, hints, question text, or per-question target time between questions. Its 40-minute main timer starts after warm-up, and mid-test adjustment changes only the internal Session 2 prompt Level; it never mutates `TrainingSelection` or localStorage.
- Full Mock may show an explicitly non-official OOM diagnostic score and estimated grade range only after completion. The calculation must disclose that it uses completion, target-duration fit, recording coverage, and optional STT/AI review evidence; it must never be framed as an official OPIc result or guarantee. A transcript cannot support pronunciation grading, so do not present text-only feedback as pronunciation or acoustic fluency assessment.
- Keep Course-neutral SEO and crawler-visible static route body. Do not hardcode one current Course into general route metadata.

## TTS Rules

- Product voices are exactly Heart, Bella, Sarah, and Sky (`af_heart`, `af_bella`, `af_sarah`, `af_sky`) unless an explicit product decision changes them.
- Fixed content is static-first: production manifest hit → WebM/Opus + precomputed peaks.
- A static miss or media failure continues to lazy Kokoro browser runtime; final fallback is system Web Speech.
- Kokoro synthesis assets are always generated at `1.00×`. STEP 4 speed changes WaveSurfer `playbackRate`; playback rate is not part of synthesis/cache identity.
- STEP 6 remains `1.00×` and its listen cap must not be bypassed by a seekable player.
- `public/generated-tts/audio/**` and `public/generated-tts/tts-manifest.json` are generator-owned, versioned runtime assets. Do not hand-edit them.
- `artifacts/tts-inventory.json` is generated but is also a direct input to static generation/validation. Keep it tracked and current.
- Do not regenerate every asset casually. The generator rescans existing hash/voice targets and creates only missing or invalid targets.
- Never perform actual prune without explicit user authorization. Use `npm run tts:validate -- --prune-dry-run` for evidence.

See `docs/TTS_AUDIO_PIPELINE.md` and `docs/decisions/001-static-first-tts.md`.

## Content Change Checklist

For a new Course or changed script/question/roleplay/preview text:

1. Update the owning `src/data/training/courses/course-N/` file or `src/lib/tts/voiceConfig.ts`.
2. Run `npm run tts:audit`; inspect the tracked inventory diff.
3. Run `npm run tts:generate`; use the browser generator to create only missing hash/voice assets.
4. Run `npm run tts:validate -- --prune-dry-run`; do not prune.
5. Run `npm run lint`, `npm run test`, `npm run build`, and `npm run verify:pages`.
6. Run `npm run docs:generate` and `npm run docs:check` when source inventory, package scripts, or docs change.
7. Run `git diff --check` and inspect the exact changed-file scope.

The full authoring sequence and current structural expectations are in `docs/CONTENT_AUTHORING.md`.

## Protected Areas

- Course × Level architecture, automatic Course discovery, selection persistence, and guard behavior
- Course 1 Advanced canonical content and its regression comparison with `src/data/scripts.ts`
- STEP 1~6 route order and training-only progress header
- Self Introduction is not STEP 7. It is a Course-neutral Level-aware single child menu under STEP 4, and a one-time warm-up before the first STEP 6 practice question. Do not add a progress slot or change the six-step order.
- STEP 4 canonical storyline, three tabs, variant continuity, OPEN/SCENE/CLOSE
- STEP 5 CORE/OPTIONAL model and Course-owned scenarios
- STEP 6 parent/child routes, Quick regression contract, Full Mock orientation and exam state machines, exam/review/report boundary, EVA, listen 0/2, Recorder, post-exam optional STT, editable transcript, AI fallback, and downloadable local HTML diagnostic report
- independent landing shell at `/` and shared AppShell behavior elsewhere
- light/dark theme, responsive navigation, keyboard focus, accessible names, loading/error states
- static-first TTS, four locked voices, generated audio/peaks/manifest, Kokoro and Web Speech fallbacks
- GitHub Pages route generation and canonical/trailing-slash behavior

## Coding Rules

- Inspect current route/component/data ownership before editing; do not infer ownership from filenames in historical or reference folders.
- Follow existing components, tokens, and patterns before adding an abstraction or dependency.
- Keep UI Korean-first with zinc/slate foundations and restrained indigo, emerald, and amber accents.
- Do not use global overflow hacks to hide layout defects.
- Keep runtime browser-only and static-host compatible. Do not add server-only code or secrets.
- Never hardcode an API key. Runtime LLM/STT settings live only in browser `localStorage`.
- Avoid unrelated refactors and broad content rewrites.
- Do not commit or push unless the user explicitly requests it.

## Validation

After code or style changes:

```bash
npm run lint
npm run test
npm run build
npm run verify:pages
git diff --check
```

After documentation, source inventory, or package-script changes:

```bash
npm run docs:generate
npm run docs:check
```

After TTS-covered content or TTS pipeline changes:

```bash
npm run tts:audit
npm run tts:validate -- --prune-dry-run
```

Run `npm run tts:generate` only when the audit shows missing/changed assets. It starts a local browser-assisted generator and is not a CI command.

## Documentation and Repository File Policy

- **Canonical docs:** human-maintained files listed under Source of Truth. Update the owning document when architecture, content workflow, TTS, routing, or deployment changes.
- **Generated inventory:** `docs/PROJECT_SNAPSHOT.md` and `artifacts/tts-inventory.json`. Regenerate with their commands; do not hand-edit.
- **Generated local report:** `docs/generated/tts-inventory.md` is recreated by `tts:audit` and ignored by Git.
- **Generated runtime assets:** `public/generated-tts/audio/**` and production manifest are generator-owned and tracked together.
- **Historical decisions/audits:** `docs/decisions/**`, `docs/AUDIT_*.md`, `docs/IMPLEMENTATION_AUDIT_RESOLUTION.md`, and `docs/TRAINING_CONTENT_QA.md`. They explain past evidence, not current ownership.
- **Reference packages:** `reference/**` is non-canonical supplied material. Never import it into production or treat it as current behavior without verifying the active source.
- **Temporary output:** test captures, staging manifests, interrupted WAV/WebM/peaks temp files, local caches, and benchmark output stay untracked.

`CLAUDE.md` and `.roo/rules/oom-project.md` are short pointers back to this guide.
